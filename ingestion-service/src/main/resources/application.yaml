spring:
  application:
    name: eventmesh-ai

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  port: 8080

logging:
  level:
    root: INFO

app:
  kafka:
    topic:
      raw-events: raw-events-topic
      dlq: dlq-topic