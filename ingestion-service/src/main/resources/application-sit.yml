spring:
  datasource:
    url: jdbc:postgresql://sit-db:5432/eventmesh_sit
    username: sit_user
    password: sit_password

  kafka:
    bootstrap-servers: sit-kafka:9092

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

logging:
  level:
    root: INFO

app:
  env: sit